/* ============================================================================
   CONFIG — el ÚNICO archivo que tenés que editar.
   ----------------------------------------------------------------------------
   Pegá acá los dos datos de TU proyecto Supabase.
   Los encontrás en:  Supabase → tu proyecto → Project Settings → Data API
     * Project URL      -> SUPABASE_URL   (algo como https://abcd1234.supabase.co)
     * anon / public key -> SUPABASE_ANON  (una cadena larga que empieza con "eyJ...")

   La "anon key" es pública por diseño (va en el navegador). Lo que protege el
   sistema es la CLAVE DE DISPOSITIVO, que va en la URL (#clave=...), no acá.
   ==========================================================================*/
const SUPABASE_URL  = "https://TU-PROYECTO.supabase.co";
const SUPABASE_ANON = "TU_ANON_KEY";
